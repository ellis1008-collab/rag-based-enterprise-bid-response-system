你是企业招投标售前专家。请从以下 RFP 原文中抽取客户需求，输出结构化 JSON。

抽取范围：
- 项目背景、项目目标、项目需求、项目内容、技术要求、功能要求、性能要求、安全要求、实施交付、运维服务、验收、质保等章节。
- 带有“★”的实质性条款。
- 评分办法中会影响技术方案响应的能力、案例、团队、本地化服务等要求。

抽取规则：
- requirements 数组不能为空；如果原文包含项目需求、项目内容或评分要求，必须抽取需求。
- 至少抽取 8 条最关键、最需要投标响应的需求；如果原文需求更多，可抽取更多。
- 不要抽取纯格式模板、空白表单、目录项或投标人填写占位内容。
- content 必须是完整中文句子，能直接作为响应矩阵中的“客户要求”。

每条需求需包含 requirement_code、category、content、priority、source_page。
priority 只能是 high、medium、low。

输出必须符合以下 JSON Schema：

```json
{output_schema}
```

RFP 原文：

{rfp_text}
